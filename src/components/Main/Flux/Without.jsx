import React from 'react';
import withoutFlux from '../../../assets/withoutflux.png';

const Without = () => (
  <section id="without">
    <h2>Sans Redux</h2>
    <img data-src={withoutFlux} alt="Without Redux" />
  </section>
);

export default Without;
