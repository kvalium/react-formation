import React from 'react';
import withFlux from '../../../assets/withflux.png';

const With = () => (
  <section id="with">
    <h2>Avec Redux</h2>
    <img data-src={withFlux} alt="With Redux" />
  </section>
);

export default With;
